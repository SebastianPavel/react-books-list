# Books App – Frontend

Small React + TypeScript application built as a coding exercise.

## Features
- Display a list of books with title, optional description and optional image
- Toggle book description visibility
- Graceful fallback for missing or invalid image URLs
- Form to add a new book with validation
- Basic accessibility (labels, ARIA attributes)
- Unit tests using Vitest and React Testing Library

## Tech Stack
- React
- TypeScript
- Vite
- Vitest + React Testing Library

## Running the app
```bash
npm install
npm run dev
